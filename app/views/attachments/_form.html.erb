<h3>Add an image</h3>
<%= form_for([@article, @attachment], html: {multipart: true}) do |f| %>
  <p>
    <%= f.label :image %><br />
    <%= f.file_field :image %>
  </p>
  <p>
    <%= f.submit 'Submit' %>
  </p>
<% end %>
